# Es6的学习
## 新增了一个基本数据类型
```
var s1=Symbol();
产生一个独一无二的值
```
## let 和const
```
声明变量的操作符

let
>不存在变量提升
>在同一个作用域下不能重复声明
>新出现了一个{}作用域

const
>用来声明一些常量，一旦声明不能更改
```
## 结构赋值
```

```
## 字符串的扩展
```
```
includes()方法 返回值：true或false
'123'.includes('1')
模板字符串 规则反引号括起来 里面的变量用${变量名}
var name='cjd'
var html='我的名字是${name}'
```
## 数值的扩展
```
Math.trunc();返回整数部分
Math.sign();判断是正数或者负数或者是0，如果是整数返回的是1，如果是负数返回的是-1，如果是0返回的是0
```
## 数组的扩展

## 对象的扩展

Object.assign(target,sources);
合并对象，把源对象合并到目标对象里面，一般用{}空对象做目标对象
对象的扩展写法

## 


